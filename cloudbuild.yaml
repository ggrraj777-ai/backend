steps:
  # Build the container image with verbose output
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '--progress=plain'
      - '-t'
      - 'gcr.io/$PROJECT_ID/drivemond-backend:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/drivemond-backend:latest'
      - '.'
    timeout: 2400s
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/drivemond-backend:$SHORT_SHA']
  
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/drivemond-backend:latest']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'gauva'
      - '--image=gcr.io/$PROJECT_ID/drivemond-backend:$SHORT_SHA'
      - '--region=europe-west1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=1Gi'
      - '--cpu=2'
      - '--timeout=300'
      - '--max-instances=10'
      - '--min-instances=0'
      - '--set-env-vars=APP_ENV=production,APP_DEBUG=false,LOG_CHANNEL=stderr,APP_URL=https://gauva-798219755346.europe-west1.run.app,ASSET_URL=https://gauva-798219755346.europe-west1.run.app,RAZORPAY_KEY_ID=rzp_test_RVmSxTBdwWng9o,RAZORPAY_KEY_SECRET=L0Q7LVHqXj1seMQut0D87m5S'

images:
  - 'gcr.io/$PROJECT_ID/drivemond-backend:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/drivemond-backend:latest'

timeout: 3600s

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
  diskSizeGb: 100
  substitution_option: 'ALLOW_LOOSE'
