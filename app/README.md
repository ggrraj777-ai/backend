# app/

Core Laravel application code.

- See `../docs/Architecture.md` for structure.
- Broadcasting channels and events: `Broadcasting/`, `Events/`, `Listeners/`.
- HTTP layer: `Http/` (controllers, middleware, requests).
- Providers: `Providers/`.
- Helpers/Lib: `Lib/`, `Library/` (autoloaded in `composer.json`).
