# Modules/

Feature modules managed by `nwidart/laravel-modules`.

- Enable/disable via `modules_statuses.json`.
- Each module typically contains `Config/`, `Database/`, `Entities/`, `Http/`, `Providers/`, `Resources/`, `Routes/`, `Lib/`.

See `../docs/Modules.md` for the overview and guidance.
